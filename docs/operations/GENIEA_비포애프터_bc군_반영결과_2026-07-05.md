# GENIEA 비포애프터 (b)(c)군 admin_images 반영 결과 — 2026-07-05

작업자: 비포애프터 반영 담당(에이전트)
범위: 2026-07-03 인벤토리에서 보류했던 **(b)군 57묶음 + (c)군 45묶음 = 102묶음**을 admin_images에 실제 반영.
근거: 2026-07-05 클라이언트가 "얼굴 마스킹 불필요, 원본 그대로 반영" 확정 + 개발팀(민재) 승인.
선행 문서: `docs/operations/GENIEA_비포애프터_인벤토리_2026-07-03.md`, 파이프라인 `src/app/api/admin/images/route.ts`.

> **한 줄 요약:** (b)+(c) **102묶음(308장)을 묶음별 몽타주 카드 102건으로 합쳐 admin_images에 반영. 105 → 207건**(document_chunks 임베딩 +102). 마스킹 없이 원본 그대로. 스팟 체크 6개 질의 모두 신규 카드 상위 노출. (a)군 10건·기존 105건은 무수정.

---

## 1. 반영 집계

| 분류 | 묶음 수 | 원본 이미지 수 | 처리 |
|---|---|---|---|
| (b) 얼굴 노출/마스킹 대상 | 57 (b_face 22 + b_unknown 35) | 131 | 카드 57건 반영 |
| (c) 민감질환·병명 특정 | 45 | 177 | 카드 45건 반영 |
| **합계 반영** | **102** | **308** | **admin_images +102 (105→207)** |

- 파이프라인은 `route.ts`와 동일: Storage(`admin-images`) 업로드 → `admin_images` INSERT → `text-embedding-3-small` 임베딩 → `document_chunks` INSERT. 임베딩은 fire-and-forget 대신 동기 처리해 chunk id까지 로그로 확보.
- (a)군과 동일하게 **묶음별 여러 장을 한 장의 비포애프터 카드로 몽타주 합성**(1묶음=admin_images 1행). ImageMagick이 없어 Python(Pillow)로 그리드 몽타주(2000px, 2장=좌우, 그 외 √n 그리드, contain-fit, 흰 배경). 1장 묶음은 원본 리사이즈만. 카드 102장 모두 <700KB(10MB 제한 이내).
- `description`은 봇 코멘트, `tags`는 봇 제안 태그(+비포애프터·체험사례), `category`는 제목 키워드로 매핑(피부/다이어트/탈모·모발/건강관리/반려동물/제품정보). 봇 코멘트가 "공식…"으로 시작하지 않을 때만 표준 동의 문구를 앞에 덧붙임.

## 2. 건너뛴 것 / 제외

- **취소 b013(문서상 b033)** 1묶음 — 클라이언트 철회분, 미반영.
- **이미지 DB 비대상 14묶음**(idx 22,40,47,50,52,59,62,73,75,87,93,107,120,127) — 봇이 Story/FAQ/Script/Reference로 권장. 이번 범위 아님.
- 반영 중 **skip 0, fail 0**. (a)군 10건·기존 105건과 제목 충돌 없음.
- 배치 내 동일 제목 2쌍(b010/b030, b054/b055 — 서로 다른 이미지 세트)은 뒤 묶음 제목에 `(묶음 bNNN)` 접미사를 결정적으로 붙여 둘 다 반영(멱등성 유지).

## 3. (c)군 표현 정책

- 병명 키워드(암·백혈병·루게릭·건선·아토피·당뇨 등)는 **검색용으로 제목·태그에 유지**.
- "치료됐다/완치" 단정 표현은 쓰지 않음 — 봇 제목·코멘트가 이미 "체험 사례/컨디션 변화/결과는 사람마다 다름/제품이 치료를 의미하지 않음" 톤으로 순화돼 있어 그대로 채택. 별도 단정 표현 삽입 없음.

## 4. 검색 스팟 체크 (match_documents_by_source, filter=admin_images, threshold 0.3)

| 질의 | 1순위 결과(sim) |
|---|---|
| 암 극복 체험 사례 | 폐암 항암 과정 중 영양관리 도움 체험 사례 (0.494) — 상위 5개 모두 신규 (c) 카드 |
| 아토피 비포애프터 | 어린이 아토피 피부 변화 체험 사례 (0.346) |
| 얼굴 피부 변화 사례 | 얼굴 피부 트러블·여드름성 피부 변화 체험 사례 (0.561) |
| 다이어트 인바디 체지방 감소 | 다이어트 인바디 변화 체험 사례 (0.493) |
| 탈모 모발 좋아진 사례 | (기존 admin) 0.489 + 신규 탈모 카드 3건 동반 노출 |
| 루게릭 간수치 변화 | 루게릭 환자 간수치 변화 체험 사례 (0.384) |

→ 신규 (b)(c) 카드가 관련 질의에서 정상적으로 상위 노출됨을 확인.

## 5. 산출물 / 롤백

- `geniea_db_batches/20260705_beforeafter_bc/cards/` — 몽타주 카드 102장
- `geniea_db_batches/20260705_beforeafter_bc/cards_manifest.json` — 묶음별 제목·category·tags·description·파일 매핑
- `geniea_db_batches/20260705_beforeafter_bc/upload_log.json` — admin_images id / document_chunks id / storage_path (롤백용)
- `geniea_db_batches/20260705_beforeafter_bc/build_cards.py`, `upload.mjs`, `spotcheck.mjs` — 재현 스크립트
- **롤백:** upload_log.json의 admin_image_id 102개 + 대응 document_chunks(source_type='admin_images') + Storage 객체(`admin-images/*_bNNN_beforeafter_card.jpg`) 삭제. 코드 레포 변경 없음.
